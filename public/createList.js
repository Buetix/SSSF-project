'use strict';

window.addEventListener('load', async() => {
    const option = document.querySelector('.reviewSelect')
    
    const getRevs = async () => {
        const data = [];
        try {
            
        } catch (e) {
            console.log(e);
        }
    }
});